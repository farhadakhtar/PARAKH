# Calibration dataset - build report

Generated by `tools/build_calibration_dataset.py`. Read-only over `Data/`; no PARAKH module was imported or modified.

## Verdict

**This dataset is NOT calibration-ready, and cannot be made so from the files available.**

`Data/` contains two files, both generated by PARAKH itself. There are no audit documents, so there is nothing to derive a label from. Every record is unlabelled and that is the correct result: the alternative was to invent labels, which the brief forbids and which would poison any calibration fitted on them.

## Counts

| metric | value | target | met |
|---|---|---|---|
| usable records | 20,000 | 5,000+ | YES |
| labelled records | 0 | 200+ | NO |
| high-confidence labels | 0 (0.00%) | - | - |
| duplicate record_id | 100 | - | - |

## Label breakdown

- positive (issue found): **0**
- negative (audited, no issue): **0**
- unlabelled (never audited): **20,000**

`label` is NULL, never 0, where no audit covered the record. Zero would assert *audited and found clean*; NULL says *never looked at*. Those are different claims and the distinction decides whether a calibration curve is meaningful.

## Class balance

Not computable - there are no labels of either class.

## Missing data

| feature | missing |
|---|---|
| budget_allocated | 16.4% |
| expenditure | 22.98% |
| utilisation_ratio | 35.55% |

## Files seen

| file | type | usable | reason |
|---|---|---|---|
| `ground_truth_ledger.json` | SEMI_STRUCTURED | False | .json read for provenance only; not a source of records (see report.md) |
| `synthetic_dataset.csv` | STRUCTURED_DATA | True | tabular .csv file |

## Processing notes

- synthetic_dataset.csv: 20,000 row(s) normalised
- no audit PDFs present in Data/; audit_labels.csv is empty by necessity, not by failure

## What was deliberately not done

**The ground-truth ledger was not used as labels.** It records injected *data-quality* noise - `missing:vendor_name`, `placeholder:date_approval`, `date_order:approval_before_proposal`. None of those is an overpayment, a ghost beneficiary or a procurement irregularity, so mapping them onto audit categories would be fabrication. The ledger's own note also states: *"NEVER join this into the corpus before Stage 7 evaluation - it leaks labels."*

**No rows were invented to reach the 200-label target.** With zero audit documents the honest count is zero.

## To make this dataset real

Place genuine audit PDFs (CAG, PSU, SEAR) in `Data/` and re-run. The parser in step 3 is implemented and functional - it extracts issue type, severity, geography and rupee amounts, and handles crore/lakh units. It has simply had nothing to read.

Even then, treat the output as **weak** labels: they are matched by state, district and year, not by record. PARAKH's Stage 8 requires `provenance='real'` and will refuse anything else.
